# Managed by Puppet
#

<%= 
  scope.function_sub_template_call([
    'rsyslogv8/config/input.erb',
    {
      'input' => {
        'port'             => @port,
        'name'             => @title,
        'type'             => @_imodule,
        'ruleset'          => @_real_ruleset_name,
        'ssl'              => @override_ssl,
        'ssl_cert'         => @override_ssl_cert,
        'ssl_ca'           => @override_ssl_ca,
        'ssl_key'          => @override_ssl_key,
        'auth'             => @remote_auth,
        'authorised_peers' => @remote_authorised_peers,
      }
    }
  ])
%>

